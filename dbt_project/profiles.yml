beneficios:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      dbname: "{{ env_var('POSTGRES_DB', 'beneficios_db') }}"
      user: "{{ env_var('POSTGRES_USER', 'beneficios') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'beneficios_pass') }}"
      schema: staging
      threads: 4
