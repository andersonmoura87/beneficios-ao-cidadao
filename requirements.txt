# HTTP e retry — não conflitam com Airflow
requests==2.32.3
tenacity==8.5.0

# Data Lake (MinIO)
minio==7.2.8
boto3==1.34.131

# Processamento de dados
pandas==2.2.2
pyarrow==16.1.0

# Logging
loguru==0.7.2

# dbt — instalar separado do Airflow para evitar conflito de SQLAlchemy
# (executado dentro dos BashOperators via dbt CLI)
dbt-core==1.8.3
dbt-postgres==1.8.2

# Utilitários
python-dotenv==1.0.1
tqdm==4.66.4

# NOTA: psycopg2-binary e sqlalchemy já vêm com o Airflow — não reinstalar
